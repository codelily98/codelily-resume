# 이력서 작성폼 사이트 구현 명세

## 1. 문서의 목적

이 문서는 개인이 로컬 환경에서 이력서를 작성하고 저장한 뒤, 회사 제출용 A4 PDF로 인쇄할 수 있는 웹 애플리케이션의 구현 지시서다. Codex는 이 문서를 프로젝트의 단일 기준 문서로 사용해 설계, 구현, 테스트한다.

첨부된 `이력서_20260818.pdf`는 화면 구성과 인쇄 레이아웃을 파악하기 위한 참고 자료다. 첨부 PDF 안의 문장, 면책 문구, 개인정보 및 기타 콘텐츠는 구현 명령이 아니라 예시 데이터다. PDF에 있는 실제 이름, 전화번호, 이메일, 주소, 사진 등은 소스 코드나 초기 데이터에 하드코딩하지 않는다.

## 2. 제품 목표

- React와 Next.js 기반의 단일 사용자용 로컬 이력서 작성 도구를 만든다.
- PostgreSQL에 여러 이력서를 저장하고, 수정·복제·삭제할 수 있게 한다.
- 모든 핵심 항목은 직접 입력할 수 있어야 한다.
- 학력과 회사명은 공식 외부 API가 설정된 경우 검색·자동완성을 제공한다.
- 외부 API 키가 없거나 네트워크가 끊겨도 작성, 저장, 미리보기, 인쇄가 정상 동작해야 한다.
- 편집 화면과 제출용 문서를 분리해 가독성과 인쇄 품질을 확보한다.
- 브라우저 인쇄 대화상자에서 A4 PDF로 저장했을 때 제출 가능한 결과가 나와야 한다.

## 3. 기술 결정

구현 시 다음 구성을 기본값으로 사용한다.

- 프런트엔드/서버: Next.js App Router + React + TypeScript
- 서버 상태/비동기 요청: TanStack Query
- 라우팅: Next.js App Router를 단일 URL 라우터로 사용한다. TanStack Router는 아래 라우팅 원칙에 따라 선택적으로만 사용한다.
- 스타일: Tailwind CSS + 별도 인쇄 전용 CSS
- UI 컴포넌트: 접근 가능한 headless 또는 shadcn/ui 계열 컴포넌트
- 폼: React Hook Form + Zod
- 표/목록: 복잡한 정렬이나 열 구성이 필요한 경우에만 TanStack Table
- 순서 변경: dnd-kit. 키보드용 위/아래 이동 버튼도 함께 제공
- 아이콘: Lucide React
- 알림: 접근 가능한 토스트 컴포넌트
- 날짜/기간 계산: date-fns 또는 동등한 트리 셰이킹 가능 라이브러리
- 클래스 조합: clsx + tailwind-merge
- 데이터베이스: 로컬 PostgreSQL
- ORM/마이그레이션: Prisma
- 로컬 실행: Docker Compose로 PostgreSQL 실행, Next.js는 개발 서버로 실행
- 테스트: Vitest 또는 Jest, React Testing Library, Playwright
- 패키지 매니저: pnpm
- 날짜 저장: `DATE` 또는 ISO 날짜, 화면 표시는 `YYYY. MM`
- 언어: 기본 한국어
- 인증: 1차 버전에서는 없음. 로컬 단일 사용자 사용을 전제로 한다.

정확한 패키지 버전은 구현 시점의 안정 버전을 사용하되 lock 파일을 반드시 커밋해 재현 가능하게 한다.

### 3.1 TanStack Query 사용 원칙

- `@tanstack/react-query`를 이력서 목록 조회, 편집 데이터 조회, 저장 mutation, 외부 API 검색, 백업 상태에 사용한다.
- 루트에 `QueryClientProvider`를 한 번만 구성한다.
- 서버에서 미리 가져온 데이터가 필요한 화면은 `dehydrate`와 `HydrationBoundary`로 전달한다.
- Query key는 한 파일에서 관리하는 key factory를 사용한다. 예: `resumeKeys.all`, `resumeKeys.detail(id)`, `resumeKeys.educations(id)`.
- 수정 후 관련 query를 정확히 갱신하거나 무효화하고 전체 캐시를 무분별하게 초기화하지 않는다.
- 자동 저장 mutation은 동일 섹션의 요청 순서를 보장하고 이전 응답이 최신 입력을 덮어쓰지 못하게 한다.
- React Hook Form의 입력 중 상태를 Query 캐시에 매 키 입력마다 복사하지 않는다. 폼이 로컬 편집 상태를 소유하고, 저장 성공 시 서버 상태를 동기화한다.
- 인쇄 화면은 항상 서버/DB의 저장 완료 데이터를 기준으로 렌더링한다. 미저장 입력이 있으면 인쇄 전에 사용자에게 알린다.
- 외부 API 검색은 검색어를 Query key에 포함하고, 짧은 `staleTime`, 재시도 제한, 요청 취소를 설정한다.
- 개발 환경에 TanStack Query Devtools를 선택적으로 제공하되 프로덕션/인쇄 화면에는 포함하지 않는다.

### 3.2 TanStack Router와 Next.js 라우팅 원칙

Next.js App Router와 TanStack Router는 모두 URL 라우팅의 소유자가 될 수 있으므로 같은 URL 트리를 동시에 관리하지 않는다.

- 기본 구현에서는 원래 요구사항인 Next.js를 유지하고 Next.js App Router를 유일한 URL 라우터로 사용한다.
- `@tanstack/react-router`를 단순히 기술 스택 수를 늘리기 위해 중복 설치하지 않는다.
- 이력서 편집 단계는 `/resumes/[id]/edit?section=profile`처럼 Next.js 경로/검색 파라미터로 표현한다.
- TanStack Router가 반드시 필요한 별도 SPA 편집기를 향후 추가한다면 독립 진입점과 독립 URL 영역으로 분리하고, 해당 영역에서는 Next.js Router를 혼용하지 않는다.
- 전체 라우팅을 TanStack Router로 전환하려면 Next.js 요구사항을 TanStack Start 또는 Vite 기반 SPA로 변경하는 별도 아키텍처 결정을 먼저 기록한다.

따라서 1차 버전의 필수 TanStack 패키지는 TanStack Query이며, TanStack Router는 Next.js와의 중복 책임을 피하기 위해 조건부 확장 항목으로 명세에 포함한다.

### 3.3 Tailwind CSS 사용 원칙

- Tailwind CSS는 편집 UI, 미리보기 공통 토큰, 반응형 레이아웃에 사용한다.
- 색상, 글꼴, 간격, 테두리 값을 CSS 변수 기반 디자인 토큰으로 정의해 화면과 인쇄의 일관성을 유지한다.
- 반복되는 긴 utility 묶음은 공통 React 컴포넌트로 추출한다. 임의의 `@apply` 남용은 피한다.
- 인쇄 규칙은 `app/globals.css` 또는 전용 `print.css`의 `@media print`와 `@page`에 명시한다.
- 동적으로 생성한 클래스 문자열은 빌드 시 누락될 수 있으므로 허용 가능한 색상과 크기를 정적 매핑으로 관리한다.
- 인쇄 화면에서 그림자, 애니메이션, hover 전용 표현은 제거한다.
- Tailwind 설정만으로 해결하기 어려운 페이지 나눔, mm 단위 여백, 인쇄 색상 보정은 일반 CSS를 사용한다.

## 4. 핵심 원칙

1. **오프라인 우선**: 외부 API는 보조 기능이며 필수 경로가 아니다.
2. **직접 입력 보장**: 자동완성 결과를 선택하지 않아도 자유 텍스트로 저장할 수 있다.
3. **편집과 출력 분리**: 편집 UI의 버튼, 입력 테두리, 도움말은 출력물에 나타나지 않는다.
4. **개인정보 보호**: 사용자가 명시적으로 API 검색을 실행한 값 외에 이력서 데이터를 외부로 전송하지 않는다.
5. **구조화된 데이터**: 반복 항목은 각각 별도 레코드로 저장하며 순서 변경과 출력 제외를 지원한다.
6. **안전한 저장**: 저장 실패를 성공처럼 표시하지 않고, 이탈 전 미저장 변경을 경고한다.
7. **휴대성**: DB 마이그레이션과 이력서 백업/복원 기능으로 다른 로컬 환경에서도 이동 가능하게 한다.

## 5. 사용자 흐름

### 5.1 첫 실행

1. 사용자가 PostgreSQL을 실행한다.
2. 환경 변수 파일을 준비하고 마이그레이션을 적용한다.
3. 개발 서버를 실행한다.
4. 빈 상태 화면에서 `새 이력서 만들기`를 누른다.

README에는 아래 명령 흐름과 Windows PowerShell 기준 설명을 포함한다.

```text
pnpm install
docker compose up -d db
pnpm prisma migrate deploy
pnpm dev
```

### 5.2 이력서 작성

1. 이력서 제목과 기본 프로필을 입력한다.
2. 필요한 섹션을 추가하고 반복 항목을 작성한다.
3. 각 섹션과 항목의 순서를 드래그 또는 위/아래 버튼으로 조정한다.
4. 자동 저장 상태를 확인하거나 `저장` 버튼을 누른다.
5. 미리보기에서 페이지 나눔과 공개 정보를 확인한다.
6. `인쇄/PDF 저장`을 눌러 A4 PDF로 저장한다.

### 5.3 외부 API 사용

1. 설정 화면에 API 키를 입력한다. 키는 서버 측 환경 변수 사용을 우선한다.
2. 학력 또는 경력 폼에서 학교명/회사명을 검색한다.
3. 결과를 선택하면 확인된 기본 정보만 폼에 채운다.
4. 검색 실패, 결과 없음, API 미설정 시 즉시 직접 입력 상태로 전환한다.
5. 외부 결과를 선택한 뒤에도 모든 값은 사용자가 수정할 수 있다.

## 6. 화면과 경로

| 경로 | 용도 | 주요 기능 |
| --- | --- | --- |
| `/` | 이력서 목록 | 새로 만들기, 최근 수정일, 열기, 복제, 삭제, 백업/복원 |
| `/resumes/new` | 신규 생성 | 제목 입력 후 생성 |
| `/resumes/[id]/edit` | 편집 | 섹션 입력, 저장 상태, 순서 변경, 공개 여부 설정 |
| `/resumes/[id]/preview` | 미리보기 | 실제 출력과 동일한 A4 페이지 미리보기 |
| `/resumes/[id]/print` | 인쇄 전용 | 자동 인쇄 옵션, 인쇄 UI만 제공, 편집 컨트롤 제거 |
| `/settings/integrations` | 연동 설정 | API 설정 상태, 연결 테스트, 데이터 전송 안내 |

존재하지 않는 이력서 ID는 404 화면을 표시한다. 삭제는 확인 절차를 거치며, 가능하면 짧은 시간 동안 되돌리기를 제공한다.

경로 이동과 검색 파라미터는 Next.js App Router가 관리한다. TanStack Query는 경로가 아니라 해당 화면에서 필요한 서버 데이터와 mutation 상태만 관리한다.

## 7. 편집 화면 레이아웃

데스크톱에서는 다음 구조를 사용한다.

- 왼쪽: 섹션 내비게이션과 완료 상태
- 가운데: 현재 섹션의 입력 폼
- 오른쪽: 독립적으로 세로 스크롤할 수 있는 축소된 실시간 미리보기 또는 `미리보기 열기` 버튼
- 상단 고정 영역: 이력서 제목, 저장 상태, 미리보기, 인쇄/PDF 저장

모바일에서는 한 열로 전환하고 미리보기는 별도 화면으로 연다. 모든 입력은 키보드만으로도 조작할 수 있어야 하며, 오류는 필드 가까이에 텍스트로 표시한다.

반복 가능한 항목에는 다음 기능을 공통으로 제공한다.

- 추가
- 수정
- 복제
- 삭제 확인
- 순서 변경
- 출력 포함/제외
- 진행 중 여부
- 비어 있는 선택 필드의 출력 생략

## 8. 이력서 섹션 및 입력 필드

### 8.1 문서 설정

- 이력서 내부 제목
- 제출용 표시 제목 또는 한 줄 소개
- 템플릿 선택: 1차 버전은 `기본 A4` 한 종류
- 강조 색상: 인쇄 가능한 제한 팔레트
- 섹션 순서
- 각 섹션 출력 포함 여부
- 최종 수정일 자동 표시 여부
- 사실 확인 문구 표시 여부와 사용자 정의 문구

### 8.2 개인정보/프로필

- 증명사진 업로드, 교체, 삭제
- 사진 자르기 및 3:4 비율 미리보기
- 이름
- 영문 이름
- 생년 또는 생년월일
- 성별: 선택 안 함, 남성, 여성, 직접 입력
- 휴대폰 번호
- 이메일
- 주소
- 웹사이트
- GitHub
- LinkedIn 또는 기타 프로필 링크
- 한 줄 소개
- 자기소개 요약

출력 공개 여부를 연락처 필드별로 설정할 수 있게 한다. 나이처럼 시간이 지나면 달라지는 값은 저장값을 하드코딩하지 말고, 생년월일이 있을 때 기준일에 따라 계산하거나 아예 출력하지 않게 한다.

### 8.3 기술 스택

- 기술명
- 분류: 언어, 프레임워크, 라이브러리, 데이터베이스, 클라우드, DevOps, IDE, 도구, 기타
- 숙련도: 선택 안 함, 입문, 초급, 중급, 고급
- 사용 기간(개월)
- 설명
- 대표 기술 여부
- 출력 순서

출력물에서는 첨부 PDF처럼 간결한 태그 목록을 기본으로 사용한다. 숙련도와 사용 기간은 사용자가 표시를 선택한 경우에만 출력한다.

### 8.4 학력

- 학교명
- 학교 구분: 고등학교, 전문대학, 대학교, 대학원, 기타
- 입학일
- 졸업일
- 재학 상태: 졸업, 졸업 예정, 재학, 휴학, 중퇴, 수료, 기타
- 전공
- 복수/부전공
- 학위
- 학점, 만점
- 소재지
- 졸업 논문/작품 제목
- 졸업 논문/작품 설명
- 외부 API 제공자와 원본 식별자(선택)
- 출력 포함 여부와 순서

학점은 숫자 범위를 검증하고 `취득 학점 <= 만점`이어야 한다. 진행 중인 학력은 종료일 없이 저장할 수 있다.

### 8.5 경력

- 회사명
- 회사 표시명
- 입사일
- 퇴사일
- 재직 중 여부
- 부서
- 직급/직책
- 고용 형태: 정규직, 계약직, 인턴, 프리랜서, 파견, 기타
- 주요 직무
- 근무지
- 회사 홈페이지
- 회사 설명
- 담당 프로젝트/업무 요약 목록
- 성과 목록
- 사용 기술 목록
- 외부 API 제공자와 원본 식별자(선택)
- 출력 포함 여부와 순서

재직 기간은 월 단위로 자동 계산하되 사용자가 표시 여부를 선택할 수 있게 한다. 경력이 겹칠 수 있으므로 총 경력은 단순 합산이 아니라 중복 월을 제거한 기간으로 계산한다. 사용자가 수동 총 경력을 입력해 덮어쓸 수도 있어야 한다.

### 8.6 경력기술서

경력기술서는 경력 항목에 연결하거나 독립 항목으로 만들 수 있다.

- 연결 경력
- 제목
- 역할
- 프로젝트/업무 배경
- 수행 업무
- 수행 과정
- 문제와 해결 방법
- 성과
- 사용 기술
- 시작일/종료일
- 링크
- 출력 포함 여부와 순서

긴 서술은 일반 텍스트 또는 제한된 서식 편집기로 입력한다. 출력 안전성을 위해 제목, 문단, 글머리표, 굵게, 링크만 허용하고 임의 HTML은 저장하거나 렌더링하지 않는다.

### 8.7 수료 교육

- 교육명
- 교육 기관
- 시작일/종료일 (년/월/일)
- 수료 여부
- 교육 시간
- 교육 내용
- 습득 기술
- 증빙 파일 첨부 (최대 5개)
- 출력 포함 여부와 순서

첨부 PDF처럼 백엔드, 프런트엔드, CI/CD 등 세부 학습 내용을 여러 문단 또는 목록으로 출력할 수 있어야 한다.

### 8.8 자격증

- 자격증명
- 발급 기관
- 취득일 (년/월/일)
- 만료일 (년/월/일)
- 자격 번호
- 점수/등급
- 증빙 파일 첨부 (최대 5개)
- 출력 포함 여부와 순서

자격 번호는 기본적으로 출력하지 않으며 사용자가 명시적으로 공개할 때만 출력한다.

### 8.9 포트폴리오

- 제목
- URL
- 설명
- 역할
- 기간
- 사용 기술
- 대표 여부
- 출력 포함 여부와 순서

출력물에는 URL 텍스트를 함께 표시해 종이로 보더라도 주소를 알 수 있게 한다. PDF에서는 클릭 가능한 링크가 유지되어야 한다.

### 8.10 병역 및 취업 우대사항

- 병역 대상 여부
- 복무 상태: 군필, 미필, 면제, 해당 없음, 기타
- 군별
- 계급
- 병과
- 입대일/전역일
- 면제 사유
- 보훈 대상 여부
- 취업보호 대상 여부
- 고용지원금 대상 여부
- 장애 여부
- 기타 우대사항
- 항목별 출력 포함 여부

민감 정보는 기본 비공개로 시작한다. 사용자가 출력 포함을 켠 값만 제출용 문서에 표시한다.

### 8.11 희망 근무조건

첨부 PDF에 존재하는 항목이므로 선택 섹션으로 제공한다.

- 고용 형태
- 희망 근무지 목록
- 희망 연봉과 `면접 후 결정` 옵션
- 희망 직무 목록
- 희망 산업 목록
- 입사 가능일
- 기타 조건

### 8.12 추가 섹션

- 어학
- 수상
- 대외활동
- 해외 경험
- 인턴
- 봉사활동
- 사용자 정의 섹션

1차 버전의 필수 범위는 8.1부터 8.11까지다. 8.12는 데이터 구조 확장성을 확보하되 일정에 따라 후속 범위로 둘 수 있다.

## 9. 첨부 PDF에서 반영할 시각 구조

첨부 자료의 내용을 그대로 복제하지 않고 다음 레이아웃 특징을 반영한다.

- 최상단에 큰 한 줄 소개
- 사진과 기본 인적사항을 한 개의 프로필 블록으로 배치
- 프로필 아래에 학력, 경력, 활동, 자격증 요약 카드 배치
- 기술 스택을 작은 태그로 표시
- 본문은 학력, 경력, 경력기술서, 교육, 자격증, 포트폴리오, 병역/우대사항, 희망근무조건 순으로 읽히게 구성
- 날짜 열과 내용 열을 분리해 긴 경력을 빠르게 훑을 수 있게 구성
- 제목은 검정, 보조 정보는 회색, 링크/기간 강조는 제한된 강조색 사용
- 테두리는 얇게, 그림자와 장식은 최소화
- 하단 사실 확인 문구는 사용자가 켠 경우에만 출력

첫 페이지의 요약 카드와 기술 태그는 데이터가 있을 때만 표시한다. 빈 섹션이나 빈 행은 인쇄물에서 완전히 제거한다.

## 10. 인쇄 및 PDF 요구사항

### 10.1 출력 방식

- `/resumes/[id]/print`는 제출용 문서만 렌더링한다.
- 인쇄 결과에는 서비스명 `Lilyume`과 개발 도구·외부 위젯을 포함하지 않는다.
- `인쇄/PDF 저장` 버튼은 새 창 또는 현재 창에서 인쇄 전용 경로를 열고 `window.print()`를 실행한다.
- 사용자는 브라우저의 `PDF로 저장` 기능으로 파일을 만든다.
- 직접 PDF 파일을 생성하는 서버 기능은 2차 범위다. 1차 버전에서는 브라우저 인쇄 결과의 품질을 우선한다.

### 10.2 페이지 규격

- 용지: A4 세로, `210mm x 297mm`
- 기본 인쇄 여백: 상하 12mm, 좌우 14mm
- 배율: 100% 기준
- 배경 그래픽을 꺼도 정보가 사라지지 않게 구성
- 기본 글자 크기: 9.5~11pt 범위
- 본문 줄 높이: 1.45 이상
- 흰 배경과 높은 명암 대비 사용
- 자가 호스팅 한글 폰트와 시스템 폴백 사용

필수 인쇄 CSS 예시:

```css
@page {
  size: A4 portrait;
  margin: 12mm 14mm;
}

@media print {
  html,
  body {
    background: #fff;
    print-color-adjust: exact;
    -webkit-print-color-adjust: exact;
  }

  .no-print {
    display: none !important;
  }

  .resume-item,
  .summary-card {
    break-inside: avoid;
    page-break-inside: avoid;
  }

  .section-title {
    break-after: avoid;
    page-break-after: avoid;
  }
}
```

### 10.3 페이지 나눔 규칙

- 섹션 제목만 페이지 마지막에 남지 않게 한다.
- 짧은 학력, 경력, 자격증 항목은 항목 내부에서 분리하지 않는다.
- 긴 경력기술서는 문단 사이에서 자연스럽게 나눌 수 있다.
- 다음 페이지로 넘어간 항목에는 불필요한 큰 상단 여백을 만들지 않는다.
- 고정 높이 컨테이너와 내용 잘림을 유발하는 `overflow: hidden`을 사용하지 않는다.
- 인쇄 시 링크, 날짜, 표 테두리, 한글 글리프가 잘리거나 겹치지 않아야 한다.
- 페이지 수는 데이터 길이에 따라 자동으로 늘어나야 한다.

### 10.4 인쇄 전 점검 UI

미리보기 화면에 다음 경고를 제공한다.

- 필수 프로필 값 누락
- 종료일이 시작일보다 빠름
- 잘못된 URL 또는 이메일
- 출력되는 민감 정보 존재
- 외부 링크 접근 불가 여부는 경고만 하고 인쇄를 막지 않음
- 빈 페이지 또는 과도한 페이지 분할 가능성

## 11. 저장 동작

- 명시적 `저장` 버튼을 항상 제공한다.
- 입력 변경 후 800~1500ms 디바운스로 자동 저장한다.
- 상태를 `저장 중`, `저장됨`, `저장 실패`, `오프라인 변경 있음`으로 표시한다.
- 자동 저장 요청은 마지막 수정본이 이전 요청 결과에 덮어쓰이지 않도록 버전 번호 또는 `updatedAt` 비교를 사용한다.
- 저장 mutation은 TanStack Query로 구현하고, 낙관적 업데이트가 실패하면 이전 캐시로 롤백한 뒤 폼 입력은 유지한다.
- 같은 섹션에 대한 자동 저장은 직렬화하거나 최신 요청만 유효하도록 요청 ID를 사용한다.
- 저장 성공 후 `resumeKeys.detail(id)`와 관련 섹션 캐시만 갱신한다.
- 페이지 이탈 시 저장되지 않은 변경이 있으면 경고한다.
- 서버와 클라이언트에서 동일한 Zod 규칙으로 검증한다.
- 오류 발생 시 입력 내용을 유지하고 재시도 버튼을 제공한다.

## 12. 데이터베이스 설계

### 12.1 공통 규칙

- 모든 기본 키는 UUID 또는 CUID를 사용한다.
- 모든 테이블에 `createdAt`, `updatedAt`을 둔다.
- 반복 항목에는 `sortOrder`, `isVisible`을 둔다.
- 이력서 삭제는 1차 버전에서 트랜잭션으로 연관 데이터를 함께 삭제한다.
- 자유 서술은 `TEXT`, 단순 목록은 별도 테이블 또는 검증된 JSON으로 저장한다.
- API 응답 전체를 저장하지 말고 필요한 표준 필드와 원본 식별자만 저장한다.

### 12.2 권장 모델

| 모델 | 역할 | 주요 필드 |
| --- | --- | --- |
| `Resume` | 이력서 루트 | title, headline, template, accentColor, sectionOrder, lastPrintedAt |
| `Profile` | 개인정보 | name, englishName, birthDate, gender, phone, email, address, links, summary, photoPath, visibility |
| `Skill` | 기술 스택 | name, category, proficiency, monthsUsed, description, featured, sortOrder, isVisible |
| `Education` | 학력 | schoolName, schoolType, startDate, endDate, status, major, degree, grade, gradeScale, thesis, sourceProvider, sourceId |
| `Experience` | 경력 | companyName, displayName, startDate, endDate, isCurrent, department, position, employmentType, jobTitle, location, description, sourceProvider, sourceId |
| `ExperienceBullet` | 경력 업무/성과 | experienceId, kind, content, sortOrder |
| `CareerDetail` | 경력기술서 | experienceId, title, role, background, process, problemSolution, outcome, startDate, endDate, url |
| `Training` | 수료 교육 | name, organization, startDate, endDate, completionStatus, hours, description, proofFiles |
| `Certification` | 자격증 | name, issuer, acquiredDate, expiresDate, credentialId, grade, proofFiles |
| `Portfolio` | 포트폴리오 | title, url, description, role, startDate, endDate, featured |
| `MilitaryPreference` | 병역/우대사항 | militaryStatus, branch, rank, specialty, startDate, endDate, exemptionReason, preferenceFlags, visibility |
| `WorkPreference` | 희망근무조건 | employmentTypes, locations, salaryType, salaryAmount, roles, industries, availableDate, notes |
| `CustomSection` | 사용자 정의 | title, content, sortOrder, isVisible |
| `IntegrationSetting` | 연동 상태 | provider, enabled, lastCheckedAt, status. 비밀 키 원문은 DB에 저장하지 않음 |

`Profile.visibility`와 민감 항목의 공개 설정은 필드별 불리언을 가진 검증된 JSON 객체로 구현할 수 있다. 금액은 정수로 저장하고 통화 단위를 별도 필드로 둔다.

## 13. 서버 API 설계

TanStack Query가 호출하는 JSON API는 Next.js Route Handler로 구현한다. Server Action과 Route Handler를 같은 CRUD 흐름에서 혼용하지 않는다. 초기 서버 렌더링과 인쇄 화면은 서버 컴포넌트에서 서비스 계층을 직접 호출할 수 있지만, 브라우저 mutation과 외부 연동 프록시는 Route Handler를 사용한다.

| 메서드 | 경로 | 용도 |
| --- | --- | --- |
| `GET` | `/api/resumes` | 이력서 목록 |
| `POST` | `/api/resumes` | 이력서 생성 |
| `GET` | `/api/resumes/[id]` | 전체 이력서 조회 |
| `PATCH` | `/api/resumes/[id]` | 문서 설정/프로필 수정 |
| `DELETE` | `/api/resumes/[id]` | 이력서 삭제 |
| `POST` | `/api/resumes/[id]/duplicate` | 이력서 복제 |
| `PUT` | `/api/resumes/[id]/sections/[section]` | 섹션 단위 저장 |
| `POST` | `/api/resumes/[id]/reorder` | 섹션/항목 순서 저장 |
| `GET` | `/api/resumes/[id]/export` | JSON과 사진을 포함한 백업 다운로드 |
| `POST` | `/api/resumes/[id]/sections/[section]/[itemId]/proofs` | 교육·자격증 증빙 파일 첨부 |
| `GET` | `/api/resumes/[id]/sections/[section]/[itemId]/proofs/[fileId]` | 증빙 파일 다운로드 |
| `DELETE` | `/api/resumes/[id]/sections/[section]/[itemId]/proofs/[fileId]` | 증빙 파일 삭제 |
| `POST` | `/api/resumes/import` | 백업 검증 및 복원 |
| `GET` | `/api/integrations/schools/search?q=` | 학교 검색 프록시 |
| `GET` | `/api/integrations/companies/search?q=` | 회사 검색 프록시 |
| `GET` | `/api/integrations/health` | 연동 설정과 연결 상태 |

모든 변경 API는 입력 검증, 존재 여부 확인, 트랜잭션, 일관된 오류 응답을 갖춰야 한다. 오류 응답에는 사용자 메시지와 개발용 코드를 구분해 넣고 비밀 키, DB 연결 문자열, 개인정보를 로그에 남기지 않는다.

## 14. 외부 API 연동 전략

### 14.1 공통 인터페이스

```ts
type SearchResult = {
  provider: string;
  externalId: string;
  name: string;
  metadata?: Record<string, string | number | boolean | null>;
};

interface DirectoryProvider {
  isConfigured(): boolean;
  search(query: string): Promise<SearchResult[]>;
}
```

- UI는 특정 제공자 응답 형식에 직접 의존하지 않는다.
- 검색어는 최소 2글자, 300ms 이상 디바운스한다.
- 요청에는 3~5초 타임아웃을 둔다.
- 실패 시 오류 안내와 `직접 입력 계속하기`를 같은 위치에 제공한다.
- 외부 결과는 캐시할 수 있지만, 이력서의 다른 개인정보와 결합해 외부로 보내지 않는다.
- API 키가 없는 상태가 정상 상태여야 한다.

### 14.2 학력 연동

우선 후보는 공공데이터포털의 한국대학교육협의회 대학알리미 대학 기본 정보 API다.

- 용도: 대학명과 대학 코드 검색/자동완성
- 제약: 인증키가 필요하며 고등교육 중심이므로 고등학교와 해외 학교를 모두 포괄하지 못한다.
- 구현: `PUBLIC_DATA_SERVICE_KEY`가 있을 때만 활성화한다.
- 응답이 XML이면 서버에서 표준 `SearchResult[]`로 변환한다.
- 결과가 없거나 지원하지 않는 학교는 사용자가 직접 입력한다.

참고: <https://www.data.go.kr/data/15158963/openapi.do>

### 14.3 회사 연동

우선 후보는 금융감독원 OpenDART의 고유번호/기업개황 데이터다.

- 용도: DART 공시대상 회사의 회사명 자동완성 및 기본 정보 확인
- 제약: `OPEN_DART_API_KEY`가 필요하고 모든 국내 회사, 해외 회사, 소규모 사업체를 포함하지 않는다.
- 구현: 회사 고유번호 목록을 수동 동기화해 로컬 검색 인덱스로 사용할 수 있다.
- 외부 연동은 회사명 입력을 돕는 기능일 뿐 사용자의 재직 사실을 인증하지 않는다.
- DART에 없는 회사는 직접 입력한다.

참고: <https://opendart.fss.or.kr/guide/main.do?apiGrpCd=DS001>

국세청 사업자등록 상태조회 API는 사업자번호를 이미 알고 있을 때 상태 확인에 유용하지만 회사명 자동완성에는 맞지 않으므로 1차 범위에 넣지 않는다.

참고: <https://www.data.go.kr/data/15081808/openapi.do>

### 14.4 로컬 이동성

- `.env.example`에는 키 이름만 기록하고 실제 키는 커밋하지 않는다.
- 키가 없어도 앱 시작과 모든 핵심 기능이 정상 동작해야 한다.
- 백업 파일에는 API 키를 포함하지 않는다.
- 다른 PC에서는 PostgreSQL 컨테이너, 마이그레이션, 백업 가져오기로 복원한다.
- 외부 API 호출은 브라우저가 아닌 Next.js 서버를 통해 실행해 CORS와 키 노출 문제를 피한다.

## 15. 사진과 파일 처리

- 허용 이미지: JPEG, PNG, WebP
- 최대 용량: 기본 5MB
- 업로드 시 MIME과 실제 파일 형식을 검증한다.
- 원본 파일명을 경로로 사용하지 않고 무작위 파일명을 생성한다.
- 로컬 `data/uploads/<resumeId>/`에 저장하고 DB에는 상대 경로만 저장한다.
- `data/`는 Git에서 제외한다.
- 삭제/교체 시 참조되지 않는 파일을 정리한다.
- 백업 시 사진을 포함하고, 복원 시 안전한 새 파일명으로 저장한다.
- 출력에서는 사진이 없어도 프로필 레이아웃이 무너지지 않아야 한다.

## 16. 백업과 복원

- 이력서 한 건을 버전이 있는 JSON 형식으로 내보낸다.
- 사진 등 자산이 있으면 JSON과 자산을 ZIP으로 묶는다.
- 백업에는 `schemaVersion`, `exportedAt`, `resume`를 포함한다.
- 가져오기 전에 파일 크기, 확장자, JSON 스키마, 자산 경로를 검증한다.
- 경로 순회 문자열과 실행 파일은 거부한다.
- 가져오기는 기존 이력서를 덮어쓰지 않고 새 ID로 복제 생성한다.
- 지원하지 않는 미래 버전은 명확한 오류를 표시한다.

## 17. 유효성 검사

- 이름과 이력서 제목은 공백만 저장할 수 없다.
- 이메일, URL, 전화번호는 합리적인 형식을 검사하되 국제 형식을 지나치게 제한하지 않는다.
- 시작일은 종료일보다 늦을 수 없다.
- `재직 중` 또는 `진행 중`이면 종료일을 비운다.
- 학점과 만점은 양수이며 학점은 만점을 초과할 수 없다.
- 정렬 순서는 서버 트랜잭션으로 한 번에 갱신한다.
- 사용자 정의 색상은 인쇄 대비 기준을 통과하는 값만 허용한다.
- 빈 문자열은 가능한 경우 `null`로 정규화한다.

## 18. 접근성 및 사용성

- 모든 입력에 연결된 라벨을 제공한다.
- 아이콘 버튼에 접근 가능한 이름을 제공한다.
- 키보드만으로 추가, 삭제, 이동, 저장, 인쇄가 가능해야 한다.
- 포커스 표시를 제거하지 않는다.
- 색상만으로 오류와 상태를 전달하지 않는다.
- 저장 상태와 오류는 스크린 리더가 인식할 수 있게 알린다.
- 삭제, 복원, 인쇄 대화상자의 포커스를 올바르게 관리한다.
- 터치 대상은 최소 40px 수준으로 만든다.

## 19. 개인정보와 보안

- 앱은 기본적으로 `localhost`에서만 사용한다.
- 서버 로그에 프로필 본문, 전화번호, 이메일, 주소, 자격 번호를 출력하지 않는다.
- 외부 API 검색에는 검색어 외의 이력서 데이터를 전송하지 않는다.
- 업로드 경로와 파일 다운로드 경로를 검증한다.
- 사용자 입력 HTML을 그대로 렌더링하지 않는다.
- 링크는 허용된 `http:`와 `https:` 프로토콜만 사용한다.
- 환경 변수와 로컬 데이터 디렉터리를 `.gitignore`에 포함한다.
- 개발용 DB 계정은 로컬 전용으로 구성하고 운영용 비밀번호처럼 재사용하지 않는다.

## 20. 상태 및 오류 화면

다음 상태를 빠뜨리지 않는다.

- 아직 이력서가 없는 빈 상태
- 데이터 로딩 중
- 저장 중/완료/실패
- DB 연결 실패
- 외부 API 미설정
- 외부 API 타임아웃/사용량 제한/검색 결과 없음
- 업로드 형식 또는 크기 오류
- 가져오기 파일 버전 오류
- 존재하지 않는 이력서
- 인쇄 팝업이 차단된 경우의 안내

외부 API 장애는 학력과 경력 저장을 차단하지 않는다.

## 21. 테스트 요구사항

### 21.1 단위 테스트

- 기간 계산과 중복 경력 월 제거
- 진행 중 항목의 종료일 처리
- 학점 검증
- URL과 이메일 검증
- 외부 API 응답 정규화
- 출력 공개 여부 필터링
- 백업 스키마 검증

### 21.2 통합 테스트

- 이력서 생성, 조회, 수정, 복제, 삭제
- 각 반복 섹션 CRUD와 순서 변경
- 자동 저장 충돌 방지
- PostgreSQL 트랜잭션 롤백
- API 키가 없는 학교/회사 검색의 수동 입력 전환
- 외부 API 타임아웃 시 저장 가능 여부
- TanStack Query mutation 성공 시 상세/목록 캐시 동기화
- 자동 저장 요청이 역순으로 끝나도 최신 입력이 유지되는지 확인
- 낙관적 업데이트 실패 시 캐시는 롤백되고 폼 입력은 유지되는지 확인
- 사진 업로드, 교체, 삭제
- 백업 후 새 ID로 복원

### 21.3 브라우저 테스트

- 빈 이력서에서 완성된 이력서까지 작성
- 새로고침 후 입력값 유지
- 숨긴 필드와 섹션이 미리보기/인쇄에서 제외
- 브라우저 인쇄 대화상자 진입
- A4 기준 페이지 캡처에서 잘림, 겹침, 빈 페이지가 없음
- 1페이지, 3페이지, 6페이지 분량의 테스트 데이터에서 페이지 나눔 확인
- Chromium을 필수 대상으로 하고 가능하면 Edge에서도 확인

### 21.4 인쇄 회귀 테스트

Playwright로 `/print` 경로의 PDF 또는 페이지 이미지를 생성해 다음을 확인한다.

- 용지가 A4인지
- 한글이 네모 문자로 깨지지 않는지
- 사진 비율이 유지되는지
- 섹션 제목이 페이지 하단에 홀로 남지 않는지
- 경력 항목이 의도치 않게 중간에서 잘리지 않는지
- 편집 버튼과 내비게이션이 출력되지 않는지
- URL이 보이고 클릭 가능한지

## 22. 완료 기준

다음 조건을 모두 만족해야 1차 버전이 완료된 것으로 본다.

- [ ] `docker compose up -d db` 후 로컬 PostgreSQL이 실행된다.
- [ ] README 절차만으로 빈 PC의 개발 환경에서 앱을 실행할 수 있다.
- [ ] 여러 이력서를 생성, 저장, 수정, 복제, 삭제할 수 있다.
- [ ] 개인정보, 기술스택, 학력, 경력, 수료 교육, 자격증, 포트폴리오, 병역사항, 경력기술서, 희망근무조건을 입력할 수 있다.
- [ ] 모든 필수 섹션에서 항목 추가, 수정, 삭제, 순서 변경, 출력 제외가 가능하다.
- [ ] 새로고침과 서버 재시작 후 데이터가 유지된다.
- [ ] API 키가 없어도 학교와 회사 정보를 직접 입력해 저장할 수 있다.
- [ ] API가 설정된 경우 학교 또는 회사 검색 결과를 폼에 반영할 수 있다.
- [ ] 외부 API 장애가 앱 시작, 저장, 미리보기, 인쇄를 막지 않는다.
- [ ] TanStack Query의 query key, mutation, 캐시 무효화 규칙이 일관되게 적용된다.
- [ ] Next.js App Router만 1차 버전의 URL을 소유하며 TanStack Router와 중복 라우팅하지 않는다.
- [ ] Tailwind CSS 화면과 인쇄 전용 CSS가 분리되어 A4 출력에 편집 UI가 나타나지 않는다.
- [ ] 개인정보 필드별 공개/비공개 설정이 인쇄 결과에 반영된다.
- [ ] A4 미리보기와 브라우저 PDF 저장 결과가 시각적으로 일치한다.
- [ ] 1~6페이지 분량에서 텍스트 잘림, 요소 겹침, 불필요한 빈 페이지가 없다.
- [ ] 한글 폰트, 사진, 링크가 PDF에서 정상 표시된다.
- [ ] 이력서 백업과 복원이 가능하고 API 비밀 키는 백업되지 않는다.
- [ ] 핵심 단위, 통합, 브라우저 테스트가 통과한다.
- [ ] 실제 개인정보가 소스 코드, seed, 로그, 테스트 스냅샷에 포함되지 않는다.

## 23. 구현 순서

1. Next.js, TypeScript, Tailwind CSS, TanStack Query, React Hook Form, Zod, 테스트 환경을 초기화한다.
2. Docker Compose PostgreSQL과 Prisma 스키마/마이그레이션을 구성한다.
3. QueryClient Provider, query key factory, API 클라이언트와 공통 오류 처리를 구성한다.
4. 이력서 목록과 생성·수정·복제·삭제를 TanStack Query 기반으로 구현한다.
5. 프로필 및 필수 반복 섹션의 폼과 저장 API를 구현한다.
6. 섹션/항목 순서 변경과 출력 공개 설정을 구현한다.
7. 첨부 PDF를 참고한 기본 A4 미리보기 템플릿을 구현한다.
8. 인쇄 전용 경로와 인쇄 CSS를 구현하고 여러 분량으로 검증한다.
9. 사진 업로드와 백업/복원을 구현한다.
10. 학교/회사 제공자 인터페이스와 수동 입력 폴백을 구현한다.
11. 접근성, 오류 상태, 자동 저장, 개인정보 로그 점검을 완료한다.
12. 전체 테스트와 README를 완성한다.

각 단계가 끝날 때 관련 테스트를 함께 추가한다. 인쇄 레이아웃은 마지막에 한 번에 맞추지 말고, 프로필·학력·경력·장문 경력기술서가 추가될 때마다 A4 렌더링을 확인한다.

## 24. 1차 범위에서 제외

- 사용자 계정과 다중 사용자 권한
- 클라우드 배포와 공개 URL 공유
- 실제 재직/학력 증명서 검증
- 전자서명
- 외부 채용 사이트 자동 제출
- AI가 개인정보를 외부 서비스로 보내 문장을 생성하는 기능
- 완전한 워드프로세서 수준의 자유 배치 편집
- 서버 측 직접 PDF 파일 생성

이 항목들은 기본 데이터 모델과 로컬 우선 원칙을 해치지 않는 범위에서 후속 기능으로 추가할 수 있다.

## 25. Codex 작업 규칙

- 구현 전에 현재 저장소 상태와 기존 변경 사항을 확인하고 사용자 변경을 덮어쓰지 않는다.
- 실제 개인정보나 첨부 PDF의 값을 샘플 데이터로 복사하지 않는다.
- seed와 테스트 fixture는 명백한 가상 정보만 사용한다.
- 기능을 작은 단위로 구현하고 각 단위의 테스트를 바로 실행한다.
- 새로운 외부 서비스가 필요하면 필수 의존성으로 추가하지 말고 제공자 인터페이스 뒤에 둔다.
- Next.js App Router와 TanStack Router가 같은 URL을 동시에 소유하게 만들지 않는다.
- TanStack Query에는 서버 상태만 두고 폼의 미저장 입력 상태를 억지로 저장하지 않는다.
- Tailwind utility가 복잡해지면 의미 있는 공통 컴포넌트와 디자인 토큰으로 정리한다.
- 외부 API의 응답 형식, 인증, 사용 한도는 공식 문서를 기준으로 구현한다.
- 인쇄 관련 변경은 브라우저 화면 확인만으로 끝내지 말고 A4 PDF 또는 페이지 이미지로 렌더링해 검증한다.
- 완료 보고에는 실행 방법, 구현한 범위, 테스트 결과, 남은 제한 사항을 포함한다.
